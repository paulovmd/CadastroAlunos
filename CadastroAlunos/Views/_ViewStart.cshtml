@{
    /*
    O arquivo _ViewStart.cshtml é utilizado para definir
    o layout principal da aplicação. Desta forma, todas as
    páginas criadas utilizarão o mesmo Layout.
    */
    Layout = "_Layout";
}